/**
 * Placeholder for now -- real simulator content (mask design, aerial
 * image, ATF/OTF, printed feature panels) comes in a later prompt.
 */
export default function Simulator() {
  return (
    <div className="flex min-h-screen items-center justify-center bg-page text-ink">
      <h1 className="text-4xl font-semibold">Simulator</h1>
    </div>
  );
}
