# Copy to .env.local (gitignored) to override. Without this set at all,
# frontend/src/lib/api.ts falls back to http://localhost:8000 -- set this
# once the backend is deployed somewhere else.
VITE_API_URL=http://localhost:8000
